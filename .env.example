# Optional: leave unset for local dev so Vite proxies /api to backend.
# Set only when the API is hosted separately, e.g. VITE_API_URL="https://api.example.com"
# VITE_API_URL="http://localhost:4000"
